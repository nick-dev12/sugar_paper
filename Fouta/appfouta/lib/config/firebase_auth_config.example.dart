/// Exemple — copiez les valeurs depuis Firebase Console (projet foouta-d1782).
///
/// **Web Client ID** : Paramètres du projet → Vos applications → App Web
/// → « ID client OAuth 2.0 » (type Web, se termine par .apps.googleusercontent.com)
///
/// **VAPID** : Cloud Messaging → Web Push certificates (clé publique).
const String kFirebaseWebClientId =
    'VOTRE_WEB_CLIENT_ID.apps.googleusercontent.com';

const String kFirebaseMessagingVapidKey = 'VOTRE_CLE_VAPID';
